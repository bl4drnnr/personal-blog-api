# build
/dist
/node_modules
/coverage

# environment — never commit real values, but keep *.example templates
.env
.env.*
!*.example

# host-only nginx allowlist (holds a real IP address)
deploy/nginx/admin-allowlist.conf

# local tooling
.claude/
CLAUDE.md
.DS_Store
*.log
